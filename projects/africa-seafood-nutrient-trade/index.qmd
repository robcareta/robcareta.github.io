---
title: "Africa's Seafood Trade & Nutrient Flows"
date: 2026-08-03
description: "A first look at how nutrients embedded in Africa's seafood trade move between income groups, and which countries are net importers vs exporters."
image: "img/sankey_extra_africa.png"
categories:
  - R
  - Trade economics
  - Fisheries
  - Nutrition
  - Africa
  - ARTIS
execute:
  echo: false
---

<p class="back-nav"><a href="../index.qmd">← Back to Projects</a></p>

::: {.callout-warning}
This is an active working paper. Figures below are preliminary and the full analysis is not yet public.
:::

## Which countries are net importers vs exporters

Pick a nutrient to see each country's trade position, ranked *relative to other African countries* rather than by absolute quantity (green = net exporter, red = net importer):

```{ojs}
mapdata = FileAttachment("nutrient_map_data.json").json()
```

```{ojs}
topojson = require("topojson-client@3")
```

```{ojs}
world = d3.json("https://cdn.jsdelivr.net/npm/world-atlas@2.0.2/countries-110m.json")
```

```{ojs}
africaIdSet = new Set(mapdata.map(d => d.iso_n3))
```

```{ojs}
countryFeatures = topojson.feature(world, world.objects.countries).features
  .filter(f => africaIdSet.has(+f.id))
```

```{ojs}
viewof selectedNutrient = Inputs.select(
  ["Protein", "Iron", "Zinc", "Calcium", "DHA", "EPA"],
  { label: "Nutrient", value: "Protein" }
)
```

```{ojs}
categoryById = new Map(
  mapdata.filter(d => d.nutrient === selectedNutrient).map(d => [d.iso_n3, d.category])
)
```

```{ojs}
nameById = new Map(mapdata.map(d => [d.iso_n3, d.country]))
```

```{ojs}
categoryDomain = ["Strong net exporter", "Net exporter", "Neutral", "Net importer", "Strong net importer"]
```

```{ojs}
Plot.plot({
  width: 700,
  height: 620,
  projection: { type: "mercator", domain: { type: "FeatureCollection", features: countryFeatures } },
  color: {
    domain: categoryDomain,
    range: ["#1a9850", "#91cf60", "#f7f7f7", "#fc8d59", "#d73027"],
    legend: true,
    label: `${selectedNutrient} trade position`
  },
  marks: [
    Plot.geo(countryFeatures, {
      fill: (d) => categoryById.get(+d.id) ?? "#e0e0e0",
      stroke: "white",
      strokeWidth: 0.6,
      title: (d) => `${nameById.get(+d.id) ?? "Unknown"}: ${categoryById.get(+d.id) ?? "No data"}`
    })
  ]
})
```

::: {.callout-note}
Categories are relative quintiles (each country's rank against other African countries), not raw trade values — deliberately coarser than the full analysis, which stays with the working paper for now.
:::

## Overview

Species-level bilateral seafood trade data lets each shipment be converted into flows of protein, iron, zinc, calcium, and omega-3 fatty acids (DHA, EPA), not just tonnage. This is a preliminary look at where those nutrients actually go, split by World Bank income group and by whether trade stays within Africa or crosses into the rest of the world.

## Nutrient flows by income group

Trade with the rest of the world (left) looks very different from trade within Africa (right). Ribbon width is proportional to nutrient volume; color denotes the exporting income group.

![](img/sankey_extra_africa.png){fig-alt="Sankey diagram of nutrient flows between African and non-African countries by income group" width="100%"}

![](img/sankey_intra_africa.png){fig-alt="Sankey diagram of nutrient flows between African countries only by income group" width="100%"}

## Data

Built from species-level bilateral trade data merged with a nutrient-content crosswalk and World Bank income classifications. Full data and methods documentation to follow with the working paper.
